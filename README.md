# probot-test

## Development

Run the test suite with:

```bash
python -m pytest
```

If `pytest` is not installed, install the project test dependencies first (for example, `python -m pip install pytest`).
